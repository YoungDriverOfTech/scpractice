spring:
  cloud:
    config:
      name: configclient
      label: master
      discovery:
        enabled: true
        service-id: configserver # 在厨厕中心里面去找这个名字的服务
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/